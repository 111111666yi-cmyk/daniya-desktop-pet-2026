# V0.49 Release Report

## 1. Scope

v0.49 is the official open source release stage for Daniya Summer Desktop Pet.
The goal of this stage is release hardening, version metadata, Windows packaging, GitHub Release Notes preparation, sensitive-file checks, independent package smoke testing, and final release reporting.

## 2. Out Of Scope

- No new feature development beyond release blockers found during validation.
- No main architecture replacement.
- No UI rewrite.
- No action system rewrite.
- No `DialogueEngine` rewrite.
- No `ProviderManager` rewrite.
- No settings center rewrite.
- No private assets in the release package.
- No `.env` in the release package.
- No `data/` in the release package.
- No `models/` in the release package.

## 3. V0.48 Blocking Issue Check

- `docs/V0.48_RC_REPORT.md`: present.
- `docs/KNOWN_ISSUES_v0.48.md`: present.
- Blocking issues: none recorded.
- Non-blocking issues: none recorded.

## 4. New Files

- `docs/GITHUB_RELEASE_NOTES_v0.49.md`
- `docs/KNOWN_ISSUES_v0.48.md`
- `docs/V0.49_RELEASE_REPORT.md`
- `pytest.ini`
- `src/version.py`

Other untracked files from prior accepted stages are part of the release snapshot, including provider/local-model/action validation modules, reports, and tests.

## 5. Modified Files

- `.gitignore`
- `README.md`
- `CHANGELOG.md`
- `assets/placeholder/*`
- `config/app_config.json`
- `config/app_config.example.json`
- `docs/release_checklist.md`
- `docs/roadmap.md`
- `pack.bat`
- `src/asset_manager.py`
- `src/config_manager.py`
- `src/pet_window.py`
- `tests/test_display_regression.py`

Additional modified files from earlier release stages remain in the release snapshot and were validated by the full test suite.

## 6. Version Updates

- `README.md`: current stable version is v0.49.
- `CHANGELOG.md`: added `v0.49 - Official Open Source Release`.
- `docs/roadmap.md`: v0.49 marked as current stable release.
- `config/app_config.json`: `version` set to `v0.49`.
- `config/app_config.example.json`: `version` set to `v0.49`.
- `src/version.py`: `APP_VERSION = "v0.49"`.
- `pack.bat`: package name set to `DaniyaSummerPet-v0.49-win-x64`.

## 7. Validation Results

- `python tools\validate_character_pack.py characters\daniya`: passed, `Character pack OK: daniya`.
- `python tools\validate_assets.py assets\private`: passed, `0 errors, 0 warnings`.
- `python tools\validate_assets.py assets\placeholder`: passed, `0 errors, 0 warnings`.
- `pytest -q`: passed, 99 tests.
- `run.bat`: startup smoke passed; process stayed running after 12 seconds.
- `pack.bat`: passed.

## 8. Release Package

- Directory: `release/DaniyaSummerPet-v0.49-win-x64/`
- Zip: `release/DaniyaSummerPet-v0.49-win-x64.zip`
- SHA256: `CAC9D97A63FA3B26222FDD98A6B91F0BD0AC5D9254A443221137EF92A35A7D19`
- Zip content check: required files present.
- Forbidden content check: no `.env`, `data/`, `assets/private/`, `models/`, `backups/`, `build/`, `dist/`, `release/test_run*`, `__pycache__/`, `*.log`, `*.spec`, or `*.broken-*`.

## 9. Independent Package Test

Test directory: `release/test_run_v0.49/`.

- Fresh unzip: passed.
- No `.env` before launch: passed.
- No `assets/private/` before launch: passed.
- No bundled `data/` before launch: passed.
- Exe startup: passed.
- Main window title: `Daniya Summer Desktop Pet`.
- Window bounds: `Left=100`, `Top=100`, `Right=292`, `Bottom=262` on a `1440x900` primary screen.
- Full window inside primary screen: passed.
- Runtime `data/` creation after launch: expected local behavior.

## 10. Fallback Tests

- No `.env`: startup smoke passed.
- Incorrect API Key: startup smoke passed, no crash.
- Correct API Key: not run; no valid release-test key was provided in this environment.
- No private assets: startup smoke passed with neutral placeholder fallback.
- Private assets copied into independent package: startup smoke passed.
- Placeholder assets: replaced with neutral abstract public placeholders.
- Broken manifest / damaged config / damaged relation data fallback: covered by existing regression tests.

## 11. UI And System Checks

- Main desktop pet window: startup smoke passed.
- Input box: enabled in default release config and covered by regression tests.
- Bubble and typewriter: covered by regression tests.
- Dragging: covered by regression tests.
- Right-click menu: covered by existing UI wiring tests.
- Settings center: automated subprocess test passed.
- Daniya read-only settings window: code path preserved; no rewrite performed.
- v0.415 dialogue chain: regression tests passed.
- v0.41 action system: regression tests passed.
- ProviderManager fallback: regression tests passed.
- Local model connection fallback: regression tests passed.
- UI main-thread network blocking protection: existing worker-thread path preserved.
- Pet offscreen blocker: fixed; startup, clamping, docking, and drag are now constrained inside the screen.

## 12. Git Safety

- `git ls-files .env data assets/private models backups dist build release/test_run __pycache__ *.log`: no tracked sensitive files.
- `.gitignore` excludes secrets, runtime data, private assets, models, backups, build output, release output, caches, logs, and broken config backups.
- `assets/private/`, `.env`, `data/`, and `models/` are not in the release zip.

## 13. Git Tag

Git tag creation result: `v0.49` created after the final v0.49 release snapshot commit.

Tag command:

```bat
git tag -a v0.49 -m "Daniya Summer Desktop Pet v0.49"
```

## 14. GitHub Release Notes Draft

See `docs/GITHUB_RELEASE_NOTES_v0.49.md`.

Title:

`Daniya Summer Desktop Pet v0.49`

Summary:

Official open source release with transparent desktop pet window, API/local fallback, action system, settings center, Daniya character pack, Provider configuration, local model fallback, neutral placeholder fallback, and sensitive-file exclusions.

## 15. Known Issues

- No known blocking issues.
- Valid API Key network behavior was not tested in this environment because no real release-test key was provided.

## 16. Release Decision

Local release validation passed.
The release is suitable for GitHub publication if the generated zip is uploaded unchanged.
