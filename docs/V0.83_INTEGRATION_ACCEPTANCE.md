# v0.83 Integration Acceptance

Date: 2026-06-12

Status: `LOCAL AUTOMATED AND PACKAGE GATES PASS / HUMAN SIGN-OFF REQUIRED`

## Scope

The v0.83 candidate integrates:

- v0.80.1 offline cinematic 24-chapter story reader;
- v0.81 opt-in pure-local growth;
- v0.82 opt-in environment awareness;
- v0.83 opt-in conversation memory and manual observation diary.

It does not add v0.84+ vision, realtime voice, multi-character orchestration, tag creation, or GitHub Release publication.

## Automated Evidence

| Gate | Result |
|---|---|
| `git diff --check` | PASS |
| config template check | PASS |
| sensitive tracked path check | PASS |
| character pack check | PASS |
| focused v0.83 tests | PASS |
| full `pytest -q` | PASS: 385 passed, 3 skipped |
| Settings Center offscreen layout | PASS at 860x640 and 1000x760; offscreen runner lacks Chinese glyph fonts |
| package build and ZIP scan | PASS |
| packaged executable smoke | PASS: process remained alive for 10 seconds and wrote runtime files only to an isolated temporary root |
| GitHub Actions | PENDING UNTIL PUSH |

## v0.83 Privacy And Behavior Results

- Long-term memory defaults to off.
- Observation diary defaults to off.
- Runtime files are under ignored `data/`.
- Long-term memory is visible and clearable.
- Observation diaries are visible and clearable.
- Provider injection uses top-k relevant records, not the entire store.
- Secret-like user text is rejected before long-term-memory storage.
- Diary generation requires a user action and a second send confirmation.
- Provider fallback is not saved as a successful diary.

## Package Evidence

Expected archive:

```text
release/DaniyaSummerPet-v0.83-win-x64.zip
```

Latest verified package evidence before the final documentation rebuild:

- entry count: 482;
- missing required entries: 0;
- forbidden entries: 0;
- config/version errors: 0;
- secret hits: 0;
- local-path hits: 0.

Record the final rebuilt archive size and SHA256 externally because changing this packaged document changes the archive hash.

## Manual Boundaries

- Real cloud Provider reply requires a user-owned key and available quota.
- Local Ollama/LM Studio diary generation requires an actually running local service and model.
- Physical mouse feel and a second mixed-DPI monitor require human hardware testing.
- SmartScreen and antivirus results are environment-specific.
- The executable is unsigned unless a real signing operation is performed.
