{
  "version": "v0.60",
  "quiet_defaults_migration": "v0.61-quiet-defaults",
  "current_character": "daniya",
  "window": {
    "start_x": 0,
    "start_y": 0,
    "always_on_top": true,
    "show_input": true,
    "opacity_percent": 100
  },
  "pet": {
    "pet_height": 96,
    "target_height": 96,
    "min_pet_height": 80,
    "max_pet_height": 160,
    "size_presets": [80, 96, 112, 128, 144, 160],
    "idle_animation_interval_ms": 10000,
    "edge_peek_enabled": false,
    "click_to_call_enabled": false,
    "states": {
      "idle": "idle",
      "speaking": "talking"
    }
  },
  "ui": {
    "bubble_max_width": 300,
    "input_min_width": 180
  },
  "chat": {
    "timeout_seconds": 20,
    "context_limit": 8,
    "temperature": 0.8,
    "max_tokens": 360,
    "fallback_reply": "Daniya is offline right now, but local fallback is available.",
    "fallback_replies": [
      "Daniya is offline right now, but local fallback is available.",
      "The cloud service is unavailable, so local fallback is active.",
      "No API reply was received; Daniya is staying with the local fallback."
    ],
    "api_error_fallback_reply": "The API request failed, so local fallback is being used.",
    "api_error_fallback_replies": [
      "The API request failed, so local fallback is being used.",
      "The provider did not return a usable response; local fallback is active.",
      "The model connection is unavailable right now, so Daniya is using local fallback."
    ]
  },
  "api": {
    "base_url": "https://api.deepseek.com",
    "model": "deepseek-chat",
    "local_mode": false
  },
  "typewriter": {
    "char_interval_ms": 35,
    "mouth_interval_ms": 180,
    "click_cooldown_ms": 500,
    "auto_next_ms": 2000,
    "auto_hide_ms": 3000
  },
  "affinity": {
    "click_cooldown_seconds": 5
  },
  "hourly_chime_enabled": false,
  "idle_chat_enabled": false,
  "idle_chat_minutes": 10,
  "reminder_enabled": true,
  "day_night_enabled": true,
  "night_start_hour": 23,
  "night_end_hour": 7,
  "behavior_enabled": true,
  "snap_to_edge_enabled": true,
  "snap_margin_px": 24,
  "keep_on_screen_enabled": true,
  "drag_return_enabled": true,
  "idle_behavior_enabled": false,
  "idle_behavior_seconds": 600,
  "double_click_enabled": true,
  "long_press_ms": 600
}
