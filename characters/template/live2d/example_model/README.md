# Live2D Example Slot

This example directory shows the public repo shape for an optional Live2D model binding.

Keep real model binaries out of the public template unless the license explicitly allows redistribution.

Use `bindings.json` to map:

- `state -> motion_group`
- `state -> expression`
- `state -> preview_sprite`
- `state -> fallback_state`

